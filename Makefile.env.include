ifeq ($(DOIIX), )
DOIIX=false
endif
ifeq ($(DOIIX), true)
MAKELIB=iix makelib
MAKELIBOPTS= 
LINK=iix link 
CC=iix compile 
CCOPTS= +O -P +D cc=-D__APPLE2GS__
else
MAKELIB=libtool
MAKELIBOPTS=-static -o 
CC=clang
CCOPTS=-g -O0 -D__MACOS__
LINK=ld 
endif

